# Test task with a property of wrong type (e.g. 'depends_on' being a str instead of a list)
some-task:
  script: package://cmstp/../../tests/config/test_task_processor/dummy_script.py
  function: null
  depends_on: some_string_instead_of_list
  args:
    allowed: null
    default: []
