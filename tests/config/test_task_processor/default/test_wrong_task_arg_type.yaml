# Test task with an arg of wrong type (e.g. 'default' being a str instead of a list)
some-task:
  type: wrong-type
  script: package://cmstp/../../tests/config/test_task_processor/dummy_script.py
  function: null
  depends_on: []
  args:
    allowed: null
    default: some_string_instead_of_list
