# Config files
Some tasks may use their own config files for lists of packages, settings, etc. For any simple lists, please use `.txt` files with one entry per line. For more complex configs, please use `.json(c)` or `.yaml` files with appropriate structure. Of course, any other file format may be used as needed by the specific task. Please add comments at the top of each file to briefly explain how to populate it.
