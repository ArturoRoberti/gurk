[project]
name = "conda"
version = "1.0.2"
description = "A gurk plugin to install Conda (Miniconda/Anaconda) and manage Conda/Mamba environments"

[project.optional-dependencies]
gurk = [
  "commentjson",
  "ruamel.yaml",
]
